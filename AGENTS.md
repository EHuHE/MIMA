# MIMA 项目约定

## 项目目标

- 解决第十一届（2026）全国高校密码数学挑战赛赛题二：固定 6 比特 APN 置换 S 盒的低延时实现。
- 同时优化理论关键路径延时 `D_theory` 与统一综合、映射和 STA 下的实际关键路径延时 `D_sta`；二者是独立目标，不假设理论最优必然带来实际最优。
- 保留理论最优候选与实际最优候选；不要为了单一指标覆盖另一类有效方案。

## 权威材料

- 赛题原文：`problem/2026密码数学挑战赛-赛题二.pdf`
- 官方环境与模板说明：`problem/yosys_test/README.md`
- 理论候选校验器：`problem/yosys_test/sim/verify_theory.py`
- 理论 JSON 模板：`problem/yosys_test/sim/theory_apn.json`
- STR、LUT、仿真与 STA 模板：`problem/yosys_test/{apnstr,lut,str,sim}/`
- 将 `problem/` 视为只读的官方基线。不要直接修改其中的 PDF、模板、校验器、门库或示例；先复制到工作目录再实验。
- 不要依赖迁移前的旧工作区。当前项目必须能够仅凭本目录内容复现。
- 规则冲突时以赛题 PDF 为准；工具行为与文字规则不一致时，记录差异并向用户报告，不要静默修改官方材料。

## 固定功能目标

- 输入为 `x = (x5 x4 x3 x2 x1 x0)`，整数编码为 `X = sum(x_i * 2^i)`；输出采用同样的位序。
- 对输入 `0x00` 到 `0x3f`，目标 S 盒输出依次为：

```text
00 36 30 0d 0f 12 35 23 19 3f 2d 34 03 14 29 21
3b 24 02 22 0a 08 39 25 3c 13 2a 0e 32 1a 3a 18
27 1b 15 11 10 1d 01 3e 2f 28 33 38 07 2b 2c 26
1f 0b 04 1c 3d 2e 05 31 09 06 17 20 1e 0c 37 16
```

- 不得改变目标真值表、输入输出位序或评价口径。

## 硬约束

- 电路必须是纯组合逻辑，不得使用触发器、锁存器、RAM、ROM、三态、IP 核或黑盒。
- 支持结构化门级 Verilog/VHDL 与可综合 RTL；优先探索结构化实现，因为赛题明确将其作为低延时推荐路径。
- 理论方案必须形成无环 DAG，并仅使用赛题表 2 与官方校验器支持的门型。
- 所有候选必须对 64 个输入穷举验证，不能用随机测试替代完整功能验证。
- 声称有效前必须同时确认：功能正确、结构合法、可综合，以及在需要比较 `D_sta` 时可完成统一 STA 流程。
- 未经可复现证明，不得声称全局最优；使用“当前已验证最优”“当前搜索范围内最优”或“候选上界”等准确表述。

## 工作目录约定

- `problem/`：不可修改的官方材料。
- `src/`：算法、搜索器、候选生成器与公共实现代码。
- `candidates/`：可独立验证的候选；每个候选使用稳定名称并保存配置或生成种子。
- `experiments/`：运行配置、日志和中间结果。长搜索必须支持检查点与恢复。
- `results/`：汇总后的机器可读指标、验证状态和比较表。
- 不提交或长期保留虚拟环境、解释器缓存、综合临时文件和重复门库副本；必要产物应能由记录的命令重新生成。

## 标准工作流

1. 先读取赛题 PDF、官方 README 和相关模板，明确本次优化的是理论延时、STA 延时，还是二者。
2. 在 `problem/` 外创建候选；记录算法、参数、随机种子、父候选和生成时间。
3. 对候选执行 64 项真值表穷举，并检查无环、单驱动、合法门型和纯组合约束。
4. 对理论 JSON 使用官方校验器：

   ```bash
   python3 problem/yosys_test/sim/verify_theory.py candidates/<candidate>/theory.json
   ```

5. 对实际实现，在候选目录中复制并适配官方 `apnstr`/`str` 流程；使用同一门库、Yosys/GHDL/OpenSTA 版本与约束比较候选。不要在官方模板目录内产生综合结果。
6. 每次有效运行至少记录：功能状态、结构状态、`D_theory_ps`、`D_sta_ns`、面积 GE、关键路径、工具版本、完整命令和日志路径。缺失指标写明原因，不得推测填充。
7. 比较候选时同时维护理论延时榜、STA 延时榜和 Pareto 前沿；先复验改进，再更新“当前最佳”。

## 验证与交付

- 修改验证器、搜索算法或候选生成逻辑后，增加最小回归测试，并用至少一个已知有效与一个已知无效样例验证。
- 若 Yosys、GHDL 或 OpenSTA 不可用，仍完成可执行的功能与理论检查，并明确把 STA 标为“未运行”，不要把工具缺失写成候选失败。
- 交付候选时同时提供结构描述、HDL（如适用）、复现命令、原始验证输出和简短指标摘要。
- 保持原始日志与汇总指标可追溯；汇总值必须能定位到具体候选和原始输出。
